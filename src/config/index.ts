export const storageConfig = {
  languageStorageKey: "lani18n",
};

export const appConfig = {
  debugMode: true,
  apiUrl: import.meta.env.VITE_API_URL,
  authTokenCookieKey: "COOKIE_TOKEN_KEY",
  authTokenEncryptionKey: "encryption",
};

export const routes = {
};
